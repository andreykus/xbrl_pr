package com.bivgroup.xbrl.instance.impl.freemaker.generator;

import com.bivgroup.xbrl.common.generators.InnerTool;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

/**
 * implemetation inner tool
 * Created by andreykus on 18.03.2017.
 */
public class InnerTemplateTool implements InnerTool {
    /**
     * logger
     */
    protected Logger logger = LogManager.getLogger(this.getClass());
}
